// This guide is to help reconstruct the secrets file in the event it is
// ever lost, destroyed, corrupted, or files for divorce.
var secrets = {};

secrets.APP_ID = /*available on FB dev admin dashboard*/;
secrets.APP_SECRET = /*available on FB dev admin dashboard*/;

module.exports = secrets;
