CREATE TABLE IF NOT EXISTS users (
uid VARCHAR(40) UNIQUE PRIMARY KEY,
name VARCHAR(32) UNIQUE,
acct_type VARCHAR(16),
session_uid VARCHAR(40),
INDEX (session_uid)
);

CREATE TABLE IF NOT EXISTS nodes (
uid VARCHAR(40) UNIQUE PRIMARY KEY,
parent_uid VARCHAR(40),
author_uid VARCHAR(40),
path_snippet VARCHAR(100),
node_snippet VARCHAR(2000),
votification INTEGER
);

CREATE TABLE IF NOT EXISTS positions (
user_uid VARCHAR(40),
node_uid VARCHAR(40),
PRIMARY KEY (user_uid)
);

CREATE TABLE IF NOT EXISTS votes (
voter_uid VARCHAR(40),
node_uid VARCHAR(40),
sentiment TINYINT,
PRIMARY KEY (voter_uid, node_uid),
INDEX (node_uid)
);

INSERT INTO users (uid, name, acct_type, session_uid) VALUES ('', 'Steverino', 'moderator', '0');
INSERT INTO nodes (uid, parent_uid, author_uid, path_snippet, node_snippet) VALUES ('start', '00000000000000-0000000000-00000000000000', '', 'Path to first node.', 'First node content.');
INSERT INTO nodes (uid, parent_uid, author_uid, path_snippet, node_snippet) VALUES ('test', 'start', '', 'Path to second node.', 'Second node content.');
INSERT INTO positions (user_uid, node_uid) VALUES ('', 'test');
